# --- Framework Web ---
# Usado para criar a API que expõe o modelo de RAG.
flask==3.0.3
flask-cors==4.0.1 # Habilita o Cross-Origin Resource Sharing (CORS) para a API Flask.

# --- Configuração e Utilitários ---
# Carrega variáveis de ambiente de um arquivo .env para configuração.
python-dotenv==1.0.1
# Serialização/desserialização de objetos JSON, otimizado para performance.
orjson==3.11.3
# Cliente HTTP para fazer requisições, por exemplo, para APIs externas.
requests==2.32.5
# Biblioteca para retentativas automáticas de operações que podem falhar.
tenacity==8.5.0
# ORM para interagir com o banco de dados (PostgreSQL).
SQLAlchemy==2.0.43
# Cliente/servidor HTTP assíncrono.
aiohttp==3.12.15
# Ajuda a converter objetos Python (dataclasses) para JSON e vice-versa.
dataclasses-json==0.6.7
# Biblioteca para parsing de arquivos YAML (usado para configs, etc.).
PyYAML==6.0.2

# --- Ecossistema LangChain ---
# Componentes da comunidade LangChain para integrações diversas.
langchain-community==0.2.10
# O núcleo do LangChain, com as abstrações principais (LLMs, Chains, etc.).
langchain-core==0.2.43
langchain-huggingface==0.0.3
# Algoritmos para dividir textos longos em pedaços menores (chunks).
langchain-text-splitters==0.2.4
langgraph==0.1.19

# --- Modelos de Embeddings (Hugging Face) ---
# Framework para trabalhar com modelos de embeddings e transformers.
sentence-transformers>=2.6.0,<2.7
# Hub para baixar modelos e datasets do Hugging Face.
huggingface-hub==0.24.6
# Biblioteca principal do Hugging Face para modelos de NLP.
transformers==4.48.3
sentencepiece==0.2.0

# --- Vector Store e Análise de Similaridade ---
# Biblioteca para busca de similaridade eficiente em vetores.
# A versão específica (CPU/GPU) é definida nos arquivos requirements-cpu/gpu.txt.
#faiss-cpu==1.7.4
# Biblioteca para cálculo rápido de similaridade entre strings.
rapidfuzz==3.9.7
# Biblioteca fundamental para computação numérica em Python.
numpy==1.26.4
# Correção de textos com erros de codificação (mojibake) muito comum em dumps web.
ftfy==6.3.1
# O PyTorch é uma dependência do sentence-transformers.
# A versão específica (CPU/GPU) é definida nos arquivos a seguir.
#torch==2.8.0

# --- Processamento de Documentos ---
# Para extrair texto de arquivos PDF.
pypdf==4.2.0
# Para extrair texto de arquivos DOCX (Microsoft Word).
python-docx==1.1.2
# Natural Language Toolkit, usado para processamento de texto como tokenização.
nltk==3.9.1

# --- LLM (Google Gemini) ---
# SDK oficial do Google para interagir com a API do Gemini.
google-generativeai>=0.7.2,<0.8

einops>=0.7

# --- Avaliação ---
# Framework para avaliação de sistemas RAG.
langchain-google-genai>=1.0.5,<2.0.0
ragas>=0.1.9
# Biblioteca para análise e manipulação de dados, usada na avaliação e nos loaders.
pandas==2.2.2

# --- Testes ---
# Framework de testes para Python.
pytest==8.2.2
# Utilitário para mocking em testes.
pytest-mock==3.14.0
psycopg2-binary==2.9.9
datasets==2.21.0
