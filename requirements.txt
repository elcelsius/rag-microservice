# --- Framework Web ---
# Usado para criar a API que expÃµe o modelo de RAG.
flask==3.0.3
flask-cors==4.0.1 # Habilita o Cross-Origin Resource Sharing (CORS) para a API Flask.

# --- ConfiguraÃ§Ã£o e UtilitÃ¡rios ---
# Carrega variÃ¡veis de ambiente de um arquivo .env para configuraÃ§Ã£o.
python-dotenv==1.0.1
# SerializaÃ§Ã£o/desserializaÃ§Ã£o de objetos JSON, otimizado para performance.
orjson==3.11.3
# Cliente HTTP para fazer requisiÃ§Ãµes, por exemplo, para APIs externas.
requests==2.32.5
# Biblioteca para retentativas automÃ¡ticas de operaÃ§Ãµes que podem falhar.
tenacity==8.5.0
# ORM para interagir com o banco de dados (PostgreSQL).
SQLAlchemy==2.0.43
# Cliente/servidor HTTP assÃ­ncrono.
aiohttp==3.12.15
# Ajuda a converter objetos Python (dataclasses) para JSON e vice-versa.
dataclasses-json==0.6.7
# Biblioteca para parsing de arquivos YAML (usado para configs, etc.).
PyYAML==6.0.2

# --- Cache ---
# Cliente Python para o banco de dados em memÃ³ria Redis.
redis==5.0.7

# --- Ecossistema LangChain ---
# Componentes da comunidade LangChain para integraÃ§Ãµes diversas.
langchain-community==0.2.10
# O nÃºcleo do LangChain, com as abstraÃ§Ãµes principais (LLMs, Chains, etc.).
langchain-core==0.2.43
# IntegraÃ§Ã£o do LangChain com modelos do HuggingFace (novo pacote 0.2+).
langchain-huggingface==0.0.3
# Algoritmos para dividir textos longos em pedaÃ§os menores (chunks).
langchain-text-splitters==0.2.4
# IntegraÃ§Ã£o do LangChain com modelos do Google Gemini.
langchain-google-genai==1.0.7

# --- Modelos de Embeddings (Hugging Face) ---
# Framework para trabalhar com modelos de embeddings e transformers.
sentence-transformers>=2.6.0,<2.7
# Hub para baixar modelos e datasets do Hugging Face.
huggingface-hub==0.24.6
# Biblioteca principal do Hugging Face para modelos de NLP.
transformers==4.48.3
# SentencePiece compatÃ­vel com os modelos multilÃ­ngues utilizados.
sentencepiece==0.2.0

# --- Vector Store e AnÃ¡lise de Similaridade ---
# Biblioteca para busca de similaridade eficiente em vetores.
# A versÃ£o especÃ­fica (CPU/GPU) Ã© definida nos arquivos requirements-cpu/gpu.txt.
#faiss-cpu==1.7.4
# Biblioteca para cÃ¡lculo rÃ¡pido de similaridade entre strings.
rapidfuzz==3.9.7
# Biblioteca fundamental para computaÃ§Ã£o numÃ©rica em Python.
numpy==1.26.4
# O PyTorch Ã© uma dependÃªncia do sentence-transformers.
# A versÃ£o especÃ­fica (CPU/GPU) Ã© definida nos arquivos a seguir.
#torch==2.8.0

# --- Processamento de Documentos ---
# Para extrair texto de arquivos PDF.
pypdf==4.2.0
# Para extrair texto de arquivos DOCX (Microsoft Word).
python-docx==1.1.2
# Natural Language Toolkit, usado para processamento de texto como tokenizaÃ§Ã£o.
nltk==3.9.1

# --- LLM (Google Gemini) ---
# SDK oficial do Google para interagir com a API do Gemini.
google-generativeai>=0.7.2,<0.8
# UtilitÃ¡rio para rearranjo de tensores (usado por alguns modelos HF).
einops>=0.7

# --- AvaliaÃ§Ã£o de RAG ---
# Framework para avaliaÃ§Ã£o de pipelines de RAG.
ragas==0.1.9
# Patch para permitir que asyncio rode dentro de ambiente sÃ­ncrono.
nest-asyncio==1.6.0
